services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./server-data:/app/server-data
      - ./cert.pem:/app/cert.pem:ro
      - ./key.pem:/app/key.pem:ro
    restart: unless-stopped